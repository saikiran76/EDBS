{
  "public": true,
  "outputDirectory": "excalidraw-app/build",
  "buildCommand": "cd excalidraw-app && yarn add cross-env vite @vitejs/plugin-react vite-plugin-svgr vite-plugin-ejs vite-plugin-pwa --dev && yarn && yarn build:app && yarn build:version",
  "installCommand": "yarn install",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Access-Control-Allow-Origin",
          "value": "https://excalidraw.com"
        },
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        }
      ]
    }
  ]
}
