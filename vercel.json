{
  "public": true,
  "outputDirectory": "excalidraw-app/build",
  "buildCommand": "cd excalidraw-app && yarn add cross-env --dev && yarn && npx vite build --mode production && yarn build:version",
  "installCommand": "yarn install",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Access-Control-Allow-Origin",
          "value": "https://excalidraw.com"
        },
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        }
      ]
    }
  ]
}
